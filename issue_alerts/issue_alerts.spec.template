# -*- mode: python ; coding: utf-8 -*-

block_cipher = None

a = Analysis(
    ['issue_alerts.py'],
    pathex=[
        r'%USERPROFILE%\\scielo_exports\issue_alerts'
    ],
    binaries=[],
    datas=[
        ('template', 'template'),
    ],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
)

pyz = PYZ(
    a.pure,
    a.zipped_data,
    cipher=block_cipher
)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name='issue_alerts',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,
    disable_windowed_traceback=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)